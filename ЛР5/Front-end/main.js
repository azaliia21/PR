// Главный файл, который запускает приложение
import { MainPage } from "./pages/main/index.js";

// Находим корневой элемент
const root = document.getElementById('root');

// Создаём и отображаем главную страницу
const mainPage = new MainPage(root);
mainPage.render();

// Добавляем сообщение в консоль
console.log('🚀 Приложение ЛР6 загружено!');
console.log('✅ Используется Fetch API с async/await');